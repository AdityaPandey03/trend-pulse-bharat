# Required for LLM categorization, Hindi translation, and summarization
ANTHROPIC_API_KEY=sk-ant-...

# Optional: override default cache TTL (in seconds). Default 600 (10 min).
TRENDS_CACHE_TTL=600

# Optional: tune how many trends to return (default 15)
TRENDS_OUTPUT_COUNT=15
